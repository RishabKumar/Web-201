#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../dist-es6/bin/dist-es6.js" "$@"
  ret=$?
else 
  node  "$basedir/../dist-es6/bin/dist-es6.js" "$@"
  ret=$?
fi
exit $ret
